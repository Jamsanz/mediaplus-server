export interface IImage {
    data: string;
    name: string;
}

export type Image = {
    data: string,
    name: string,
}

export interface img {
    data: string;
    fileName: string;
}

export type Iimg = {
    data: string;
    fileName: string;
}